const inquirer = ('inquirer');


const html = require('');

var fs = require("fs");{
    fs.writeToFile(filename, data, error => {
        error ? console.log(error) : console.log('Page generated Successfully!');
    });
};

